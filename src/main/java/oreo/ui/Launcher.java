package oreo.ui;

import javafx.application.Application;

/** Launches the Oreo JavaFX application. */
public class Launcher {
    /** Starts the JavaFX runtime. */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
